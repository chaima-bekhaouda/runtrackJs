// Declare the "afficherJoursSemaines" function
function afficherJoursSemaines() {
    // Array containing the days of the week
    const joursSemaines = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];

    // Loop through the array to display each day
    for (let i = 0; i < joursSemaines.length; i++) {
        console.log(joursSemaines[i]);
    }
}

// Call the "afficherJoursSemaines" function to display the weekdays
afficherJoursSemaines();

